# ============================================================
# Resume Builder — Database Configuration
# ============================================================
# This file keeps all database credentials OUT of Java code.
# Change values here without recompiling the application.
# ============================================================

# JDBC driver class name (MySQL 8.x Connector/J)
db.driver=com.mysql.cj.jdbc.Driver

# Connection URL: protocol://host:port/database
db.url=jdbc:mysql://localhost:3306/resume_builder

# Database login credentials — change these to match your setup
db.username=root
db.password=sh1ru2ti@

# Optional: connection pool settings (for future use)
db.pool.size=5
