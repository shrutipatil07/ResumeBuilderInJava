# ============================================================
# Spring Boot — Application Configuration
# ============================================================
# This file is loaded by Spring Boot automatically.
# Database connectivity continues to use the existing
# ConfigLoader + DatabaseManager (config.properties) for now.
# ============================================================

# Server port — Spring Boot's embedded Tomcat will listen here
server.port=8080

# Application name — shows in logs and actuator endpoints
spring.application.name=resume-builder-api
